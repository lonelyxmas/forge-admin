<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mdframe.forge.plugin.capability.controlplane.mapper.AiCapabilityMapper">
    <sql id="BaseColumns">
        id, tenant_id, capability_code, protocol_tool_name, capability_name, description,
        source_type, source_key, source_version, current_version, schema_checksum,
        behavior, risk_level, required_actor_type, visibility, publish_status, enabled, del_flag,
        create_by, create_time, create_dept, update_by, update_time
    </sql>
    <select id="selectPage" resultType="com.mdframe.forge.plugin.capability.controlplane.domain.AiCapability">
        SELECT <include refid="BaseColumns"/>
        FROM ai_capability
        WHERE tenant_id = #{tenantId} AND del_flag = 0
        <if test="keyword != null and keyword != ''">
            AND (capability_code LIKE CONCAT('%', #{keyword}, '%')
              OR capability_name LIKE CONCAT('%', #{keyword}, '%')
              OR source_key LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="publishStatus != null and publishStatus != ''">
            AND publish_status = #{publishStatus}
        </if>
        ORDER BY update_time DESC, id DESC
    </select>
    <select id="selectTenantById" resultType="com.mdframe.forge.plugin.capability.controlplane.domain.AiCapability">
        SELECT <include refid="BaseColumns"/> FROM ai_capability
        WHERE tenant_id = #{tenantId} AND id = #{id} AND del_flag = 0 LIMIT 1
    </select>
    <select id="selectByCode" resultType="com.mdframe.forge.plugin.capability.controlplane.domain.AiCapability">
        SELECT <include refid="BaseColumns"/> FROM ai_capability
        WHERE tenant_id = #{tenantId} AND capability_code = #{capabilityCode} AND del_flag = 0 LIMIT 1
    </select>
    <select id="selectByToolName" resultType="com.mdframe.forge.plugin.capability.controlplane.domain.AiCapability">
        SELECT <include refid="BaseColumns"/> FROM ai_capability
        WHERE tenant_id = #{tenantId} AND protocol_tool_name = #{toolName} AND del_flag = 0 LIMIT 1
    </select>
    <select id="selectGrantOptions"
            resultType="com.mdframe.forge.plugin.capability.controlplane.mapper.model.CapabilityGrantOptionRow">
        SELECT c.id, c.capability_code, c.capability_name, c.current_version,
               c.source_type, c.behavior, c.risk_level, c.required_actor_type,
               c.publish_status, c.enabled, v.policy_snapshot
        FROM ai_capability c
        LEFT JOIN ai_capability_version v
          ON v.tenant_id = c.tenant_id
         AND v.capability_id = c.id
         AND v.version = c.current_version
         AND v.del_flag = 0
        WHERE c.tenant_id = #{tenantId} AND c.del_flag = 0
        ORDER BY CASE WHEN c.publish_status = 'PUBLISHED' AND c.enabled = 1 THEN 0 ELSE 1 END,
                 c.capability_name ASC, c.id ASC
    </select>
</mapper>
