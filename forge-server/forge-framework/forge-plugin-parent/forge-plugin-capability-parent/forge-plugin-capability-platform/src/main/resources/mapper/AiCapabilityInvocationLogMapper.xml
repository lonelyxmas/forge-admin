<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mdframe.forge.plugin.capability.controlplane.mapper.AiCapabilityInvocationLogMapper">
    <sql id="BaseColumns">
        id, tenant_id, request_id, client_id, client_code, capability_id, capability_code,
        capability_version, actor_type, actor_user_id, service_user_id, active_org_id, result_status, result_code,
        error_code, failure_stage, error_message, schema_path, trace_id, duration_ms, del_flag,
        create_by, create_time, create_dept, update_by, update_time
    </sql>
    <insert id="insertIdempotent">
        INSERT INTO ai_capability_invocation_log (
            id, tenant_id, request_id, client_id, client_code, capability_id, capability_code,
            capability_version, actor_type, actor_user_id, service_user_id, active_org_id, result_status, result_code,
            error_code, failure_stage, error_message, schema_path, trace_id, duration_ms, del_flag,
            create_by, create_time, create_dept, update_by, update_time
        ) VALUES (
            #{log.id}, #{log.tenantId}, #{log.requestId}, #{log.clientId}, #{log.clientCode},
            #{log.capabilityId}, #{log.capabilityCode}, #{log.capabilityVersion},
            #{log.actorType}, #{log.actorUserId}, #{log.serviceUserId}, #{log.activeOrgId},
            #{log.resultStatus}, #{log.resultCode},
            #{log.errorCode}, #{log.failureStage}, #{log.errorMessage},
            #{log.schemaPath}, #{log.traceId}, #{log.durationMs}, 0,
            #{log.createBy}, COALESCE(#{log.createTime}, NOW()), #{log.createDept},
            #{log.updateBy}, COALESCE(#{log.updateTime}, NOW())
        )
        ON DUPLICATE KEY UPDATE id = ai_capability_invocation_log.id
    </insert>
    <update id="updateResultByRequestIdentity">
        UPDATE ai_capability_invocation_log
        SET result_status = #{log.resultStatus},
            result_code = #{log.resultCode},
            error_code = #{log.errorCode},
            failure_stage = #{log.failureStage},
            error_message = #{log.errorMessage},
            schema_path = #{log.schemaPath},
            trace_id = #{log.traceId},
            duration_ms = #{log.durationMs},
            update_by = COALESCE(#{log.updateBy}, update_by),
            update_time = COALESCE(#{log.updateTime}, NOW())
        WHERE tenant_id = #{log.tenantId}
          AND request_id = #{log.requestId}
          AND client_id = #{log.clientId}
          AND client_code = #{log.clientCode}
          AND capability_id = #{log.capabilityId}
          AND capability_code = #{log.capabilityCode}
          AND actor_type = #{log.actorType}
          AND actor_user_id = #{log.actorUserId}
          <!-- 租户拦截器的 JSqlParser 无法解析 MySQL NULL 安全等号运算符，使用标准等价写法。 -->
          AND (service_user_id = #{log.serviceUserId}
               OR (service_user_id IS NULL AND #{log.serviceUserId} IS NULL))
          AND active_org_id = #{log.activeOrgId}
          AND del_flag = 0
    </update>
    <sql id="InvocationViewColumns">
        log.id,
        log.request_id,
        log.client_id,
        log.client_code,
        log.capability_id,
        capability.capability_name,
        log.capability_code,
        log.capability_version,
        log.actor_type,
        log.actor_user_id,
        actor.username AS actor_username,
        actor.real_name AS actor_real_name,
        log.service_user_id,
        service_user.username AS service_username,
        service_user.real_name AS service_real_name,
        log.active_org_id,
        log.result_status,
        log.result_code,
        log.error_code,
        log.failure_stage,
        log.error_message,
        log.schema_path,
        log.trace_id,
        log.duration_ms,
        log.create_time,
        log.update_time
    </sql>
    <sql id="InvocationViewJoins">
        FROM ai_capability_invocation_log log
        LEFT JOIN ai_capability capability
          ON capability.tenant_id = log.tenant_id
         AND capability.id = log.capability_id
        LEFT JOIN sys_user actor
          ON actor.tenant_id = log.tenant_id
         AND actor.id = log.actor_user_id
        LEFT JOIN sys_user service_user
          ON service_user.tenant_id = log.tenant_id
         AND service_user.id = log.service_user_id
    </sql>
    <select id="selectPage"
            resultType="com.mdframe.forge.plugin.capability.controlplane.vo.CapabilityInvocationDetailVO">
        SELECT <include refid="InvocationViewColumns"/>
        <include refid="InvocationViewJoins"/>
        WHERE log.tenant_id = #{tenantId}
          AND log.del_flag = 0
        <if test="clientId != null">AND log.client_id = #{clientId}</if>
        <if test="requestId != null and requestId != ''">
            AND log.request_id LIKE CONCAT('%', #{requestId}, '%')
        </if>
        <if test="capabilityCode != null and capabilityCode != ''">
            AND log.capability_code = #{capabilityCode}
        </if>
        <if test="capabilityKeyword != null and capabilityKeyword != ''">
            AND (log.capability_code LIKE CONCAT('%', #{capabilityKeyword}, '%')
                 OR capability.capability_name LIKE CONCAT('%', #{capabilityKeyword}, '%'))
        </if>
        <if test="actorKeyword != null and actorKeyword != ''">
            AND (
                <if test="actorUserId != null">log.actor_user_id = #{actorUserId} OR</if>
                actor.username LIKE CONCAT('%', #{actorKeyword}, '%')
                OR actor.real_name LIKE CONCAT('%', #{actorKeyword}, '%')
            )
        </if>
        <if test="resultCode != null and resultCode != ''">AND log.result_code = #{resultCode}</if>
        ORDER BY log.create_time DESC, log.id DESC
    </select>
    <select id="selectByRequestId" resultType="com.mdframe.forge.plugin.capability.controlplane.domain.AiCapabilityInvocationLog">
        SELECT <include refid="BaseColumns"/> FROM ai_capability_invocation_log
        WHERE tenant_id = #{tenantId} AND request_id = #{requestId} AND del_flag = 0 LIMIT 1
    </select>
    <select id="selectDetail"
            resultType="com.mdframe.forge.plugin.capability.controlplane.vo.CapabilityInvocationDetailVO">
        SELECT <include refid="InvocationViewColumns"/>
        <include refid="InvocationViewJoins"/>
        WHERE log.tenant_id = #{tenantId}
          AND log.id = #{id}
          AND log.del_flag = 0
        LIMIT 1
    </select>
</mapper>
