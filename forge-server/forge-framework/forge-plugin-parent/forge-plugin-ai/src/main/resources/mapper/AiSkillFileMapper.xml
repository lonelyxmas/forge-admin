<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mdframe.forge.plugin.ai.skill.mapper.AiSkillFileMapper">
  <sql id="fileColumns">
    id, tenant_id, skill_id, file_path, file_content, encoding,
    create_by, create_time, create_dept, update_by, update_time, del_flag
  </sql>

  <select id="selectBySkillId" resultType="com.mdframe.forge.plugin.ai.skill.domain.AiSkillFile">
    SELECT <include refid="fileColumns" />
    FROM ai_skill_file
    WHERE del_flag = '0' AND skill_id = #{skillId}
    ORDER BY file_path
  </select>
</mapper>
