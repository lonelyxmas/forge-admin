<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mdframe.forge.plugin.ai.skill.mapper.AiSkillMapper">
  <sql id="skillColumns">
    id, tenant_id, skill_name, skill_code, description, version, status,
    create_by, create_time, create_dept, update_by, update_time, del_flag
  </sql>

  <select id="selectSkillPage" resultType="com.mdframe.forge.plugin.ai.skill.domain.AiSkill">
    SELECT <include refid="skillColumns" />
    FROM ai_skill
    WHERE del_flag = '0'
    <if test="keyword != null and keyword != ''">
      AND (skill_name LIKE CONCAT('%', #{keyword}, '%')
        OR skill_code LIKE CONCAT('%', #{keyword}, '%')
        OR description LIKE CONCAT('%', #{keyword}, '%'))
    </if>
    <if test="status != null and status != ''">
      AND status = #{status}
    </if>
    ORDER BY create_time DESC
  </select>

  <select id="countByCode" resultType="int">
    SELECT COUNT(1) FROM ai_skill
    WHERE del_flag = '0' AND skill_code = #{skillCode}
    <if test="excludeId != null">
      AND id != #{excludeId}
    </if>
  </select>

  <select id="selectEnabledByCode" resultType="com.mdframe.forge.plugin.ai.skill.domain.AiSkill">
    SELECT <include refid="skillColumns" />
    FROM ai_skill WHERE skill_code = #{skillCode} AND status = '0' AND del_flag = '0' LIMIT 1
  </select>
</mapper>
